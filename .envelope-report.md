status: DONE
commit: 692ad74a (feat/parsimony-relax-wiring)
tests: 3/3 new envelope-wiring tests pass (test_envelope_wiring.py); 13/13 passed on affected pre-existing suites (test_structural_revive.py, test_pack_from_state.py, test_baseline_parsimony_integration.py, test_baseline_parsimony_composite.py) with slow/full-sim tests correctly deselected via `-m "not slow"`; import/doc-shape sanity check confirmed `envelope: True` reaches EcoliPackStep's config through baseline_parsimony.
concerns:
- Deviated one line from the literal spec: in pack_step.py, `locations` is passed to `pack_from_state` as a keyword (`locations=locations`) rather than positionally, to avoid breaking the existing integration test's `_stub_pack_from_state(out_dir, name, counts, volume_fl, **kwargs)` stub (a 5th positional arg would TypeError against it). Behavior is identical either way.
- A stray full-simulation slow test (`test_pre_division_snapshot`, pre-existing, unrelated to this change) was inadvertently included in an early broad test sweep and ran to its 120s timeout before I re-ran with `-m "not slow"`; no simulation was run intentionally per instructions.
- Two pre-existing untracked files (`.relax-wiring-report.md`, `.relax-wiring.diff`) were left in the worktree, not committed — unrelated to this task.

FIX (post-review): Critical bug — select_ingredients's CURATED loop was
overwriting the tuple's hand-authored `region` (e.g. rna_polymerase's
"fiber", which seats it along the DNA via fiber_pack) with the
compartment-derived "surface"/"interior", forcing RNAP into the cytoplasm
every run. Fixed: CURATED loop now keeps the declared `region` unchanged
and only derives `compartment` from `locations.get(ckey, "cytoplasm")`
(falling back to "cytoplasm" for synthetic ids like "70S_ribosome" whose
ckey isn't a string). Auto-expand monomer loop and lipid unchanged.
Extended test_select_ingredients_sets_compartment_and_region to assert
rna_polymerase.region == "fiber" post-select — verified it actually
exercises the CURATED path (not silently skipped): rna_polymerase resolves
to (region="fiber", compartment="cytoplasm").
commit (fix): 5c9e1e17
tests (fix): 3/3 pass (tests/test_envelope_wiring.py)
